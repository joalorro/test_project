This repo is a test server that supports the [dwm-client](https://github.com/joalorro/dwm-client) (Draw With Me client) application

### Getting Started

Run `npm install` followed by `npm run start:dev` or `npm start`.
